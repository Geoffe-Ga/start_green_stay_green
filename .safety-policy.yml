# Safety vulnerability ignore policy
# Only add entries here with clear justification and tracking issue

security:
  # Ignore specific vulnerabilities with justification
  ignore-vulnerabilities: {}
